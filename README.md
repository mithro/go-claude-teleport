# go-claude-teleport

`claude-teleport` moves an in-progress Claude Code session — transcript,
sidecar state, git repository/worktree and tmux window — from one machine
to another over ssh, and resumes it there.

Design and implementation are in progress; see `docs/superpowers/specs/`.
